---
import Layout from "../layouts/Layout.astro";
import Container from "@components/Container.astro";
import TextSpacerComponent from "@components/tools/TextSpacer";

const title = "Text Spacer";
const description = "Automatically add spaces between Chinese and English characters.";
---

<Layout title={title} description={description}>
  <Container>
    <div class="mt-10 flex flex-col gap-8">
      <div class="animate">
        <h1 class="text-2xl font-semibold text-black dark:text-white">
          Text Spacer (中英文混排美化) 📝
        </h1>
        <p class="mt-1 text-black/60 dark:text-white/60">
          Automatically add spaces between Chinese and English/Numbers. Perfect for social media posts.
        </p>
      </div>

      <div class="animate w-full">
        <TextSpacerComponent client:load />
      </div>

      <div class="animate mt-8 prose dark:prose-invert">
        <h3>Features</h3>
        <ul>
          <li>
            <strong>Auto Space:</strong> Automatically insert whitespace between CJK (Chinese, Japanese, Korean) and half-width English, numbers, symbols.
          </li>
          <li>
            <strong>Clear Empty Lines:</strong> Remove unnecessary empty lines to make text more compact.
          </li>
          <li>
            <strong>Full to Half Width:</strong> Convert full-width punctuation (like `，` `！` `（`) to half-width (like `,` `!` `(`).
          </li>
        </ul>
      </div>
    </div>
  </Container>
</Layout>
